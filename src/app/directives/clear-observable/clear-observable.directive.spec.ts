import { ClearObservable } from './clear-observable.directive';

describe('ClearObservableDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearObservable();
    expect(directive).toBeTruthy();
  });
});
